<li class="text-dark">
  <%= link_to user.name, user, :class => 'text-white' %>
  <% if current_user.admin? && !current_user?(user) %> ||
     <%= link_to "delete", user, method: :delete,
                                  data: { confirm: "You sure?" }, :class => 'text-white' %>
  <% end %>
</li>
